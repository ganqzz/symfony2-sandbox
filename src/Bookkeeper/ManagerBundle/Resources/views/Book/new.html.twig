{% extends 'BookkeeperManagerBundle:_layout:bookkeeper.html.twig' %}
{% block title -%}   hoge   {% endblock %}

{% block body -%}{# "-" trims leading/trailing whitespaces #}
    <h1>Create New Book</h1>

    {{ form(form, {attr: {novalidate: 'novalidate'}}) }}

    <ul>
        <li><a href="{{ path('book') }}">Book Manager</a></li>
    </ul>
{% endblock %}
